<h2>Dados do candidato</h2>
<hr>
<h4>Nome: <%= @candidate.name %></h4>
<dl>
  <dt> Email: <%= @candidate.email%></dt>
  <dt> Calular: <%= @candidate.cell_phone %></dt>
  <dt> CPF: <%= @candidate.cpf %></dt>
  <dt> Biografia resumida: <%= @candidate.short_biography %></dt>
  <dt> Formação academica: <%= @candidate.academic_training %></dt>
</dl>

  
  <%= link_to 'Editar perfil', edit_candidate_path(id: @candidate.id) %> <br><br>
  <%= link_to 'Minhas vagas', my_jobs_candidate_path(id: @candidate.id) %> <br><br>
